export const metadata = {
  title: 'Checkout',
  description: 'Complete your AB Book Shop order with Cash on Delivery.',
  robots: { index: false, follow: false },
  alternates: { canonical: '/checkout' },
};

export default function CheckoutLayout({ children }) {
  return children;
}
